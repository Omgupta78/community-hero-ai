// Google Maps Platform key (client-side, referrer-restricted — safe to ship,
// like the Firebase web key). Paste your Maps JavaScript API key below.
//
// If left blank, the app automatically falls back to Leaflet + OpenStreetMap
// (free, no key) so the map always works.
//
// IMPORTANT: in Google Cloud Console → Credentials, restrict this key by
// HTTP referrer to: http://localhost:5173/* and your deployed https://*.run.app/*
window.GOOGLE_MAPS_KEY = ''
