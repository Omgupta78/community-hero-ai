# Pre-approve install scripts for native binary packages.
# esbuild, sharp, and workerd all need postinstall scripts to
# download their platform-specific binaries — this is standard
# behaviour for these packages and safe to allow.
allow-scripts=true
